# Zoho CRM — twl_campaign function execute URL (includes auth_type=apikey&zapikey=...)
# Generate from: Zoho CRM → Setup → Developer Space → Functions → twl_campaign → REST API
ZOHO_CRM_FUNCTION_EXECUTE_URL=
